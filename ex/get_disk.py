#!/usr/bin/python

import psutil
import pprint

p = psutil.disk_partitions()
disk_info{}

for i in p:
    disk_info[p[0][0]]  <- FILL OUT DICTIONARY
    DO LOTS OF STUFF
    return disk_info

pprint.pprint(disk_info)
